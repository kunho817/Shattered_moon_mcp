import { Server } from '@modelcontextprotocol/sdk/server/index.js';

export async function setupResources(server: Server): Promise<any[]> {
  const resources: any[] = [];

  // Add resource handlers here when needed
  
  return resources;
}