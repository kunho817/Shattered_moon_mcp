import { Server } from '@modelcontextprotocol/sdk/server/index.js';

export async function setupPrompts(server: Server): Promise<any[]> {
  const prompts: any[] = [];

  // Add prompt handlers here when needed
  
  return prompts;
}